# Codex integration

Run `components/hooks/fast-feedback/fast-feedback.ps1` directly after edits or
wire it to a verified Codex event surface supported by the current environment.
This repository does not publish unverified provider configuration.

Codex should continue to run broader project checks through `verify-change`.
